{% comment %} 
            Accepts:
              - image:
              - height:
              - text:
{% endcomment %}
<div class="flex flex-col items-center justify-start">
  {% comment %} IMAGE {% endcomment %}
  <div class="p-10">
    {% if image != blank %}

      {{ image | image_url: height: height | image_tag }}
    {% else %}
      Wybierz obraz
    {% endif %}
  </div>

  {% comment %} CONTENT {% endcomment %}
  <div class="p-10 pt-0">
    {{ text }}
  </div>
</div>